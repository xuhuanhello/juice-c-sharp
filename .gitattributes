# Unity .gitattributes Template
# Source: https://github.com/gitattributes/gitattributes/blob/master/Unity.gitattributes
# Last synced: 2026-02-04
# License: MIT
#
# This template is maintained from the gitattributes/gitattributes repository
# and distributed under MIT License.

# Define macros (only works in top-level gitattributes files)
[attr]lfs               filter=lfs diff=lfs merge=lfs -text
[attr]unity-json        eol=lf linguist-language=json
[attr]unity-yaml        merge=unityyamlmerge eol=lf linguist-language=yaml

# Optionally collapse Unity YAML files on GitHub diffs
# [attr]unity-yaml        merge=unityyamlmerge text linguist-language=yaml linguist-generated

# Unity source files
*.cginc                 text
*.compute               text linguist-language=hlsl
*.cs                    text diff=csharp
*.hlsl                  text linguist-language=hlsl
*.raytrace              text linguist-language=hlsl
*.shader                text

# Unity JSON files
*.asmdef                unity-json
*.asmref                unity-json
*.index                 unity-json
*.inputactions          unity-json
*.shadergraph           unity-json
*.shadersubgraph        unity-json

# Unity UI Toolkit files
*.tss                   text diff=css linguist-language=css
*.uss                   text diff=css linguist-language=css
*.uxml                  text linguist-language=xml linguist-detectable

# Unity YAML files
*.anim                  unity-yaml
*.asset                 unity-yaml
*.brush                 unity-yaml
*.controller            unity-yaml
*.flare                 unity-yaml
*.fontsettings          unity-yaml
*.giparams              unity-yaml
*.guiskin               unity-yaml
*.lighting              unity-yaml
*.mask                  unity-yaml
*.mat                   unity-yaml
*.meta                  unity-yaml
*.mixer                 unity-yaml
*.overrideController    unity-yaml
*.playable              unity-yaml
*.prefab                unity-yaml
*.preset                unity-yaml
*.renderTexture         unity-yaml
*.scenetemplate         unity-yaml
*.shadervariants        unity-yaml
*.signal                unity-yaml
*.spriteatlas           unity-yaml
*.spriteatlasv2         unity-yaml
*.terrainlayer          unity-yaml
*.unity                 unity-yaml

# "physic" for 3D but "physics" for 2D
*.physicMaterial        unity-yaml
*.physicsMaterial2D     unity-yaml

# Exclude from GitHub stats and diffs: third-party plugins, packages lock file
Assets/Plugins/**           linguist-generated
Packages/packages-lock.json linguist-generated

# Unity LFS
*.cubemap               lfs
*.unitypackage          lfs

# 3D models
*.3dm                   lfs
*.3ds                   lfs
*.blend                 lfs
*.blend1                lfs
*.c4d                   lfs
*.collada               lfs
*.dae                   lfs
*.dxf                   lfs
*.FBX                   lfs
*.fbx                   lfs
*.jas                   lfs
*.lws                   lfs
*.lxo                   lfs
*.ma                    lfs
*.max                   lfs
*.mb                    lfs
*.obj                   lfs
*.ply                   lfs
*.skp                   lfs
*.stl                   lfs
*.ztl                   lfs

# Audio
*.aif                   lfs
*.aiff                  lfs
*.it                    lfs
*.mod                   lfs
*.mp3                   lfs
*.ogg                   lfs
*.s3m                   lfs
*.wav                   lfs
*.xm                    lfs

# Video
*.asf                   lfs
*.avi                   lfs
*.flv                   lfs
*.mov                   lfs
*.mp4                   lfs
*.mpeg                  lfs
*.mpg                   lfs
*.ogv                   lfs
*.wmv                   lfs

# Images
*.bmp                   lfs
*.exr                   lfs
*.gif                   lfs
*.hdr                   lfs
*.iff                   lfs
*.jpeg                  lfs
*.jpg                   lfs
*.pict                  lfs
*.png                   lfs
*.psd                   lfs
*.tga                   lfs
*.tif                   lfs
*.tiff                  lfs
*.webp                  lfs

# Compressed Archive
*.7z                    lfs
*.bz2                   lfs
*.gz                    lfs
*.rar                   lfs
*.tar                   lfs
*.zip                   lfs

# Compiled Dynamic Library
*.dll                   lfs
*.pdb                   lfs
*.so                    lfs

# Landed UPM native plugins are ordinary Git blobs, never LFS pointers.
# `binary` alone does not unset the filter supplied by `*.so lfs` above.
Packages/datachannel-unity/Plugins/**/*.so -filter binary

# Fonts
*.otf                   lfs
*.ttf                   lfs

# Executable/Installer
*.apk                   lfs
*.exe                   lfs

# Documents
*.pdf                   lfs

# ETC
*.a                     lfs
*.reason                lfs
*.rns                   lfs

# Spine export file for Unity
*.skel.bytes            lfs

# Exceptions for .asset files such as lightning pre-baking
LightingData.asset     binary

# ---- datachannel-unity 构建脚本：强制 LF ----
# GitHub runner 镜像装的 Git 带**系统级** core.autocrlf=true，且 actions/checkout
# 不覆盖它 —— 于是 Windows runner 上 checkout 出来的脚本是 CRLF，会让
# `set -o pipefail` 报 invalid option name、`cut` 把 versions.lock 读成 "v0.24.5\r"。
# 在仓库级治，不在 workflow 里改 core.autocrlf：仓库规则对**本机 Windows 开发者**
# 同样生效，job 级设置只保 CI。（CMake 对 CRLF 免疫，.cmake 不需要这条。）
native/scripts/*.sh      text eol=lf
native/scripts/*.py      text eol=lf
native/versions.lock     text eol=lf

# ---- datachannel-unity 原生插件：**不进 LFS** ----
# 上面的模板把 *.dll / *.so / *.a 都设成了 lfs。本包的插件二进制必须**逐条撤销**
# 那些规则 —— 它们是采用者唯一的交付物，而 LFS 恰恰会毁掉唯一的交付路径。
#
# 实际发生过（v0.1.0 就是坏的）：UPM 用 git URL 装包时若 LFS 没能取到对象，
# 会**静默**检出 132 字节的指针文本，Unity 读到它报
# "expected x64 architecture, but was Unknown architecture"。
# 无 error 无 warning，与「文件根本没构建出来」不可区分 —— 正是 CONTRIBUTING
# 第一原则点名的病，只不过这次咬的是采用者而不是我们。
#
# LFS 存在的理由是仓库体积，而 #54 实测全矩阵一次刷新约 20 MB —— 这个量级上
# 那个理由本就不成立，却拿它换掉了包能不能装。决策与理由见 SPEC §10。
#
# `-filter` 是**这里最关键的一个字**：上面的 `[attr]lfs` 宏展开成
# `filter=lfs diff=lfs merge=lfs -text`，而 `binary` 宏只等于 `-diff -merge -text`
# —— 它**碰不到 filter**。只写 `binary` 的话 *.dll / *.so 仍然走 LFS，
# 而 *.dylib 会因为模板里没有这条而"看起来修好了"。三个平台表现不一致，
# 恰恰是这个 bug 最初被误读成「只有 Windows 坏了」的原因。
Packages/datachannel-unity/Plugins/**/*.dll        -filter binary
Packages/datachannel-unity/Plugins/**/*.dylib      -filter binary
Packages/datachannel-unity/Plugins/**/*.so         -filter binary
Packages/datachannel-unity/Plugins/**/*.a          -filter binary
# *.jslib 是 JS 文本，本来就不该进 LFS
Packages/datachannel-unity/Plugins/**/*.jslib      text eol=lf
# Symbols~/ 是同一批采用者会拿到的文件（崩溃还原），同样不能走 LFS。
# 模板另有 `*.pdb lfs`；Windows 的 PDB 只住在这里，Plugins/ 里没有。
Packages/datachannel-unity/Symbols~/**/*.dll       -filter binary
Packages/datachannel-unity/Symbols~/**/*.dylib     -filter binary
Packages/datachannel-unity/Symbols~/**/*.so        -filter binary
Packages/datachannel-unity/Symbols~/**/*.a         -filter binary
Packages/datachannel-unity/Symbols~/**/*.pdb       -filter binary
